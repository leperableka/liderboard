leader.vesperfin.com {
    # Compress responses
    encode zstd gzip

    # API → backend
    handle /api/* {
        reverse_proxy backend:3000
    }

    # All other requests → frontend (React SPA)
    handle {
        reverse_proxy frontend:80
    }
}
